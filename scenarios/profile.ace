HttpClient

profilescenarios

scenarios 

RouteToProfile
	GIVEN loginscenarios.Login
	WHEN common.RouteToProfileView
	THEN
		expectedState 
			username common.rootContainer.mainView.profileView.username should be "username-${testId}"
			email common.rootContainer.mainView.profileView.email should be "info@anfelisa.de"
