	/********************************************************************************
	 * generated by de.acegen
	 ********************************************************************************/
	
	
	

	import SynchronousCommand from "../../ace/SynchronousCommand";
	import Event from "../../ace/Event";
	import * as AppUtils from "../../../src/AppUtils";
	import * as AppState from "../../../src/AppState";
	
	export default class AbstractValidateCommand extends SynchronousCommand {
	    constructor() {
	        super("registration.ValidateCommand");
	    }
	
	    initCommandData(data) {
	        data.email = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "email", "value"]
	        )
	        ;
	        data.emailInvalid = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "email", "emailInvalid"]
	        )
	        ;
	        data.username = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "username", "value"]
	        )
	        ;
	        data.available = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "username", "available"]
	        )
	        ;
	        data.password = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "password", "value"]
	        )
	        ;
	        data.passwordMismatch = AppState.get(
	        	["rootContainer", "mainView", "registrationView", "passwordRepetition", "passwordMismatch"]
	        )
	        ;
	        data.outcomes = [];
	    }

		addOkOutcome(data) {
			data.outcomes.push("ok");
		}
		
		publishEvents(data) {
			const events = [];
			const actionsToBeTriggered = [];
			if (data.outcomes.includes("ok")) {
				events.push(new Event('registration.ValidateOkEvent'));
			}
			
			this.publish(events, data);
		  	AppState.stateUpdated();
			this.trigger(actionsToBeTriggered);
	
		}

	}
	
	
	
	/******* S.D.G. *******/
	
	
	
