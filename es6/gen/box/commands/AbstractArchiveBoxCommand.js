/********************************************************************************
 * generated by de.acegen 1.5.6
 ********************************************************************************/




import AsynchronousCommand from "../../ace/AsynchronousCommand";
import TriggerAction from "../../ace/TriggerAction";
import * as AppUtils from "../../../src/app/AppUtils";
import InitBoxesForDayAction from "../../../src/box/actions/InitBoxesForDayAction";

export default class AbstractArchiveBoxCommand extends AsynchronousCommand {
    constructor() {
        super("box.ArchiveBoxCommand");
    }
    
    initCommandData(data) {
        data.outcomes = [];
    }

	addOkOutcome(data) {
		data.outcomes.push("ok");
	}

	execute(data) {
	    return new Promise((resolve, reject) => {
	    	let payload = {
	    		boxId : data.boxId,
	    		archived : data.archived
	    	};
			AppUtils.httpPut(`${AppUtils.settings.rootPath}/box/archive`, data.uuid, true, payload).then(() => {
				this.handleResponse(data, resolve, reject);
			}, (error) => {
				data.error = error;
				this.handleError(data, resolve, reject);
			});
	    });
	}

    publishEvents(data) {
		if (data.outcomes.includes("ok")) {
			new TriggerAction().publish(
				new InitBoxesForDayAction(), 
					{
					}
			)
		}
    }

}



/******* S.D.G. *******/



