	/********************************************************************************
	 * generated by de.acegen
	 ********************************************************************************/
	
	
	

	import SynchronousCommand from "../../ace/SynchronousCommand";
	import Event from "../../ace/Event";
	import * as AppUtils from "../../../src/AppUtils";
	import * as AppState from "../../../src/AppState";
	
	export default class AbstractFilterActiveCardListCommand extends SynchronousCommand {
	    constructor() {
	        super("box.FilterActiveCardListCommand");
	    }
	
	    initCommandData(data) {
	        data.selectedLastQualityFilters = AppState.get(
	        	["rootContainer", "mainView", "allActiveCardsView", "selectedLastQualityFilters"]
	        )
	        ;
	        data.activeCardList = AppState.get(
	        	["rootContainer", "mainView", "allActiveCardsView", "activeCardList"]
	        )
	        ;
	        data.outcomes = [];
	    }

		addOkOutcome(data) {
			data.outcomes.push("ok");
		}
		
		publishEvents(data) {
			const events = [];
			const actionsToBeTriggered = [];
			if (data.outcomes.includes("ok")) {
				events.push(new Event('box.FilterActiveCardListOkEvent'));
			}
			
			this.publish(events, data);
		  	AppState.stateUpdated();
			this.trigger(actionsToBeTriggered);
	
		}

	}
	
	
	
	/******* S.D.G. *******/
	
	
	
